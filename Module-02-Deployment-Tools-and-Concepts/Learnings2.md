Key Learnings

Understood the difference between containers and virtual machines in terms of architecture, performance, and resource usage.

Learned the practical differences between Podman and Docker, including daemonless and rootless execution.

Successfully ran and managed containers using Podman commands.

Deployed a static website using GitHub Pages and understood automatic deployment on push.

Built REST APIs using FastAPI with proper routing and async support.

Understood HTTP methods (GET, POST, PUT, DELETE) and REST principles.

Tested API endpoints using FastAPI’s built-in Swagger documentation.

Deployed a FastAPI application to Vercel using serverless architecture.

Learned Vercel free-tier limitations such as timeout and lack of persistent storage.

Used ngrok to expose a local development server to the public internet.

Managed sensitive data securely using environment variables instead of hardcoding secrets.

Configured CORS in FastAPI to enable frontend-backend communication across domains.

Gained understanding of modern full-stack deployment workflow from development to production.
